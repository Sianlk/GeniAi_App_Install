def add_to_cart(user_id, product_id, quantity):
    print(f"User {user_id} added {quantity} of {product_id} to cart")
    # TODO: Connect to inventory, pricing, and checkout systems
    return "Added to cart"
