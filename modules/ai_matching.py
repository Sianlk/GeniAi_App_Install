def match_patients_to_doctors(patient_profile, doctor_profiles):
    print("Running AI matching engine...")
    # TODO: Use ML to match based on specialty, location, rating
    matched_doctor = doctor_profiles[0] if doctor_profiles else None
    return matched_doctor
